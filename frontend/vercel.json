{
  "buildCommand": "npm run tw:build && npx ng build --configuration=production",
  "outputDirectory": "dist/frontend/browser",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
