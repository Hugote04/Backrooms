spring:
  application:
    name: lurking
  profiles:
    active: dev

server:
  port: 8080

# JWT secret de Supabase: Settings → API → JWT Settings → JWT Secret
app:
  jwt:
    secret: ${SUPABASE_JWT_SECRET:}
